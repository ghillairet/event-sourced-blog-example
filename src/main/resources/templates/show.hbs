{{> head.hbs}}

<section class="container">
    <h1>Post: {{post.content.title}}</h1>
    <p>{{post.content.body}}<em>{{post.content.author}}</em></p>
    <p>
        <a class="btn" href="/posts/{{post.id}}/edit">Edit...</a>
        <a class="btn" href="/">Back</a>
    </p>
</section>

{{> footer.hbs}}